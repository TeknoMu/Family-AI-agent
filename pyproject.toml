[project]
name = "family-ai-agent"
version = "0.1.0"
description = "Private multi-domain AI agent for family use"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
